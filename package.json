{
  "devDependencies": {
    "husky": "^9.0.11"
  },
  "scripts": {
    "prepare": "husky",
    "build": "npm run build --prefix lambda",
    "predeploy": "npm run build",
    "deploy": "ask deploy"
  }
}
