{
  "devDependencies": {
    "husky": "^9.0.11"
  },
  "scripts": {
    "build": "npm run build --prefix lambda",
    "deploy": "ask deploy",
    "start": "npm run start --prefix lambda",
    "predeploy": "npm run build",
    "prepare": "husky"
  }
}
