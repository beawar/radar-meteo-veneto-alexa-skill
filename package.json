{
  "devDependencies": {
    "husky": "^9.0.11"
  },
  "scripts": {
    "prepare": "husky",
    "build": "npm run build --prefix lambda",
    "deploy": "npm run build:production --prefix lambda && ask deploy"
  }
}
